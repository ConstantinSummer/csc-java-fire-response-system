# Σημειώσεις έκδοσης

## v1.0.0 - πρώτη τεκμηριωμένη βασική έκδοση διδασκαλίας

Πρώτη πλήρης έκδοση του **Greek Fire Response Coordination System**, μιας εφαρμογής κονσόλας σε Java για τη βιβλιοθήκη projects του CSC (Computer Science Center). Επίπεδο: Intermediate.

### Τι περιλαμβάνεται

- Εκτελέσιμη εφαρμογή κονσόλας με αριθμημένο μενού, προφορτωμένα δεδομένα δείγματος (14 συμβάντα, 10 σταθμοί, 38 πόροι) και επικύρωση εισόδου που δεν τερματίζει ποτέ το πρόγραμμα σε λανθασμένη είσοδο.
- Οι έντεκα απαιτούμενες λειτουργίες: καταγραφή συμβάντος (τοποθεσία, σοβαρότητα, ημερομηνία και ώρα, κατάσταση), διαχείριση πόρων, ανάθεση πόρων, απόρριψη μη έγκυρων αναθέσεων, αλλαγή κατάστασης μέσω μηχανής καταστάσεων, λίστα ενεργών συμβάντων, εμφάνιση διαθέσιμων και αναπτυγμένων πόρων, αναζήτηση και φιλτράρισμα με ορατούς αλγορίθμους, στατιστικά ανά σοβαρότητα, κατάσταση και περιοχή, και αξιοποίηση πόρων.
- Αντικειμενοστρεφής σχεδιασμός που καλύπτει κλάσεις και αντικείμενα, encapsulation, constructors, μεθόδους στιγμιοτύπου και static μεθόδους, overloading, μονοδιάστατους και δισδιάστατους πίνακες, composition, inheritance, polymorphism, enums, επικύρωση και διαχείριση εξαιρέσεων. Όλη η αποθήκευση γίνεται σε απλούς πίνακες (χωρίς `ArrayList`, `HashMap`, streams ή βάσεις δεδομένων).
- Αυτοματοποιημένα tests: 70 tests JUnit 5 σε 8 κλάσεις (έγκυρη και μη έγκυρη ανάθεση, όρια χωρητικότητας, μεταβάσεις κατάστασης, στατιστικά, αναζήτηση και φιλτράρισμα, μοντέλο domain, δεδομένα δείγματος, μενού κονσόλας).
- Δίγλωσση τεκμηρίωση (`README.md` στα ελληνικά, `README.en.md` στα αγγλικά), καταγεγραμμένη συνεδρία τερματικού στο `docs/examples/`, 15 ερωτήσεις ανασκόπησης και 7 προκλήσεις επέκτασης (καμία δεν έχει υλοποιηθεί).

### Επίδραση στο setup

- Απαιτεί JDK 17 ή νεότερο και Maven 3.6.3 ή νεότερο. Δεν χρειάζεται ρύθμιση παραμέτρων, βάση δεδομένων ή πρόσβαση στο δίκτυο κατά την εκτέλεση.
- Build και tests με `mvn test`· δημιουργία πακέτου με `mvn package` και εκτέλεση με `java -jar target/csc-java-fire-response-system-1.0.0.jar`.

### Γνωστοί περιορισμοί

- Τα δεδομένα διατηρούνται μόνο στη μνήμη και χάνονται όταν τερματιστεί το πρόγραμμα (σκόπιμα· δείτε την πρόκληση επέκτασης για αποθήκευση και φόρτωση).
- Η αποθήκευση χρησιμοποιεί πίνακες σταθερής χωρητικότητας (60 συμβάντα, 12 σταθμοί και σταθερός αριθμός πόρων ανά σταθμό)· το πρόγραμμα εμφανίζει σαφές μήνυμα όταν φτάσει ένα όριο.

### Μετάβαση

Καμία. Πρόκειται για την πρώτη έκδοση.
